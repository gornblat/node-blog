// basic express server setup

var express = require('express'); // i guess this corresponds to whatever's in the package.json
var app = express();
var port = 3000;

app.get // method that's necessary to display a View!

app.listen(port, () => {
    console.log ('Server listening on '$port'')
})

// EJS config follows

app.engine



var express = require('express');
var app = express();
